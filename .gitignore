# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# C++ / CMake build artifacts
build/
*.o
*.obj
*.exe
cpp_cores/simulator.exe

# IDE / personal config
.vscode/
.idea/

# Runtime / captured game state (local only)
python_app/state/
python_app/state/**/*.json
python_app/state/**/*.jsonl
python_app/state/raw_dumps/

# Diagnostic / research leftovers
python_app/dev_scratch/

# AI working notes (kept locally, not uploaded)
AGENTS.md

# Data backups
*.bak
python_app/data/cardDB_old_434.json

# Export logs (regenerated by refresh_data.ps1)
*.log

# Duplicate export outputs (live copies live in python_app/data/)
python_app/tools/uma_export/cardDB.json
python_app/tools/uma_export/umaDB.json

# Temporary: README not uploaded yet (per user)
README.md

# Local upload checklist (review artifact, not part of the project)
上傳清單.txt